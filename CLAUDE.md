# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repo is

Production tooling for the **Phlo Mandatory AI Course** — short animated explainer videos in Phlo's "AI Ops Learn" series, watched by all staff (clinical, ops, engineering, design, commercial, support). It is **not** an application: there is nothing to deploy, no test suite, no lint step. Each "build" produces an animated diagram that gets screen-recorded (Loom) with face-cam + narration and uploaded to AI Ops Learn.

Phlo is a regulated UK digital pharmacy. The safety guardrails in the skill (below) are non-negotiable, not stylistic.

## Build & record workflow (current)

**Excalimate — the animation tool this repo was originally built around — is no longer used.** It's broken, and the team records differently now: a single hand-drawn **static** Excalidraw scene that you pan and zoom through on screen while narrating. No MCP server, no MP4 export, no keyframes.

For each video:

1. **Generate the scene with a Python script.** Each video folder has a thin `build*.py` that imports the shared engine (`from excalidraw_kit import *`), defines its scene + beat scaffold, and calls `finish(out, max_w, total_w)` to validate + write. The engine — palette, element factory, fun primitives, reusable illustrations, the validate/write tail — lives once in the repo-root `excalidraw_kit.py`; each `build*.py` holds only the composition (scene + scaffold) and any bespoke one-off illustrations. A deterministic `random.seed` keeps re-runs identical. Don't hand-place raw elements. Canonical example: `videos/claude/3-artefacts/build_excalidraw.py`.
   ```bash
   python3 videos/claude/3-artefacts/build_excalidraw.py   # build one video (finds the kit via a sys.path walk-up)
   python3 build_all.py                                    # rebuild EVERY video, then run the Style-B guard
   ```
   `finish()` self-checks before writing: palette discipline, no stray frames, text-overflow, and text/text collisions (printed as warnings). `build_all.py` rebuilds all videos and runs the **Style-B guard**, which hard-fails on the Style A signature (frames, or typed non-hand fonts) so the editorial style can never silently return; off-palette colours are warnings.

2. **Eyeball it with the PIL preview.** There's no Excalidraw CLI, so the repo-root `preview.py` rasterises a scene to a PNG — approximate (flat fills, macOS hand-font stand-ins for the Virgil hand font) but enough to check composition, colour, overlap and text overflow without opening the app.
   ```bash
   python3 preview.py videos/claude/3-artefacts/3-artifacts.excalidraw out.png            # whole board
   python3 preview.py videos/claude/3-artefacts/3-artifacts.excalidraw out.png XMIN XMAX  # close-up x-window
   ```

3. **Record in Loom.** Open the `.excalidraw` at excalidraw.com (or the desktop app), full-screen the canvas, and pan/zoom beat-by-beat, left to right, while narrating. The beats are spaced with wide gaps (the `GAP` constant) precisely so you can frame one beat at a time on a 14" laptop without neighbours peeking in — the whitespace *is* the camera.

## Design system (current house style)

The style is **one flowing, illustrated, hand-drawn journey** — modelled on `videos/ai-foundations/1-what-is-ai/llm_explainer.excalidraw` (the preferred reference). The canonical build is `videos/claude/3-artefacts/`. Rules:

- **No frames, no boxes.** Beats are separated by whitespace only. **No connector spine / no inter-beat arrows / no baseline** — beats read as one picture through consistent shape and rhythm, not a wavy through-line (removed: it read as clutter).
- **White background** (`viewBackgroundColor: #ffffff`). **No colour-band washes behind beats** — the background stays white; colour comes from the *elements*.
- **All text hand-drawn** in the Virgil hand font (`fontFamily: 1`, the kit's `HAND`), `roughness: 1`, `lineHeight: 1.4` (roomy). No typed sans/mono.
- **Lively Excalidraw palette**, colour-coded per beat: violet / orange / green / blue / red / teal / yellow / indigo strokes, each with a matching pastel fill and an ultra-light tint. Colour comes from **vivid elements** — accent headings, saturated sticky notes, coloured illustrations — not from background panels.
- **Prominent but unpolished headings.** A big hand title in the beat's accent colour with a lively hand-drawn underline. **No step-number circle, no highlighter sweep behind the title, and no decorative sparkles/twinkles anywhere** (they read as AI slop). Use the kit's `heading()`.
- **Charming primitive illustrations** (sketched Claude face, chat-left/Artifact-panel-right window, colour-coded object doodles) carry visual interest on hero beats; light rotation jitter on notes/chips. Prefer hand-drawn icons over emoji.
- **Uniform beat dimensions.** Every beat occupies the SAME slot — content ≈ 1120 wide × 980 tall (~1.15 : 1, the shape of the "Words left, a thing right" beat), `GAP = 800` between slots — so each beat frames identically and fits a 14" MacBook screen when you pan to it. Stack side-by-side pairs vertically and grids into ~square layouts rather than spreading wide; an inherently-wide flow/diagram beat may run a little wider, within reason. Body/explanation text stays ink/grey for legibility; accents are for headings, labels, shapes and fills.
- **Live-demo cut-aways.** Where the presenter drops out to the live Claude desktop app, mark it on the board with a `demo_badge(x, y, "show in Claude desktop app: …")`, and use `[CUT TO CLAUDE DESKTOP]` / `[BACK TO BOARD]` cues in the matching script. The flowing per-beat layout supports cutting away and returning to the same beat with no structural change.

Keep British English, hyphens not em-dashes, and fictional identifiers only. **Examples must be generic, not Phlo-specific** — no Phlo branding/logo, and no patient/clinical specifics. Use everyday business scenarios (a customer reply, a budget calculator, a team tone guide, Support / Ops) so a video is reusable. Product proper nouns stay exact: Claude, Artifact, Project, Skill, MCP, Connector, Cowork, Claude Desktop. (This supersedes SKILL.md's older "use *Patient*" vocabulary rule.)

## Legacy Excalimate pipelines (kept for reference, not used)

Earlier videos were authored by driving Excalimate. These are retained as historical reference but are **not** the current workflow:

1. **Live MCP build (`build.mjs`)** — a Node script that pushed elements + keyframes + camera frames live onto the Excalimate canvas over HTTP. Example: `videos/ai-foundations/2-ai-benefits-and-cons/build.mjs`.
2. **Compile-from-files (`scripts/compile_excalimate.py`)** — compiled a static `.excalidraw` + a `.excalimate.json` animation plan into a `.checkpoint.json` for the Excalimate UI. Example trio: `videos/ai-foundations/1-what-is-ai/llm_explainer.{excalidraw,excalimate.json,checkpoint.json}`.

The launcher scripts `scripts/make-learn-video.sh` and `scripts/start-excalimate.sh` start the `@excalimate/mcp-server` MCP and belong to these legacy pipelines only.

**The shared engine lives in the repo-root `excalidraw_kit.py`; build scripts import it.** This replaced the older "each build is self-contained and embeds its own helpers" convention — the engine was duplicated across videos and copies drifted (it's how a couple of off-style files once crept in). Now: the *look* (palette, primitives, illustrations, validate/write) is centralised in the kit; each video's `build*.py` keeps only its *composition* (scene + the per-video beat scaffold: `OX`/`WID`/`ACCENT`, `beat_head`) plus any bespoke one-off illustrations. When adding a new video, copy the closest existing `build*.py` as a scene template — it already imports the kit. A change to the kit re-styles every video on next `build_all.py`; the design is intended to be stable, so kit edits are rare and deliberate.

## File layout

```
excalidraw_kit.py             # shared Style B engine: palette, factory, primitives, illustrations, validate/finish
build_all.py                  # rebuild every video + run the Style-B guard
preview.py                    # PIL rasteriser for eyeballing a scene (takes a scene path + optional x-window)
videos/<series>/<NN-slug>/    # one folder per video; thin build*.py + .excalidraw + plan/script live together
                              # (a video made of several component scenes keeps one build*.py per scene here)
.claude/skills/SKILL.md       # the phlo-learn-videos skill (visual + safety conventions)
scripts/                      # session launchers + compile_excalimate.py
```

Series seen so far: `ai-foundations/` (Module 1 concepts), `claude/` (Module 2 Claude walkthrough), `ai-training/` (the day-by-day training sessions — `3-prompting/` is **Day 3 — Prompting & CRISPE Framework**, `4-projects/` is **Day 4 — Claude Projects**, `7-skills/` is **Day 7 — Skills**, `8-cowork/` is **Day 8 — Cowork** `10-design/` is **Day 10 — design work with Claude**, `11-design-workflows/` is **Day 11 — Claude Design, job by job** and `12-design-md/` is **Day 12 — design.md**), and a stubbed `copilot/`. Each video folder also tends to hold a narration/voiceover script (`*.md` or `*-script`) and an `animation-plan.md` documenting the build so it can be rebuilt or re-exported by hand. As of 2026-09-11 **nine `claude/` videos have a Style-B `build*.py` + `.excalidraw`** — all twelve of the original Module 2 set were built and a thirteenth (`13-chatgpt-to-claude`) was added later, then `2-projects/` moved to `ai-training/4-projects/` as Day 4, `4-skills/` to `ai-training/7-skills/` as Day 7 and `11-cowork/` to `ai-training/8-cowork/` as Day 8 and `12-design/` to `ai-training/10-design/` as Day 10, leaving gaps at 2, 4, 11 and 12 in the `claude/` numbering (gaps, not lost work; the remaining folders were **not** renumbered); the two built `ai-foundations/` videos have Style-B-guard-clean `.excalidraw` scenes but no `build*.py` (they were authored by hand / via the legacy pipelines); **Day 11 (`ai-training/11-design-workflows/`) and Day 12 (`ai-training/12-design-md/`) are both new work, not moves — neither has a `claude/` predecessor.** Day 12 was built as Day 11 and **renumbered 11 → 12 on 2026-09-14** to make room for the new Day 11; Day 10's beat 2 pointer chip was regenerated to read "Day 12, design.md" at the same time. **Claude PowerPoint is reserved for Day 13** — the number is claimed, the folder move from `videos/claude/7-claude-powerpoint/` has NOT been made. That number **reuses the retired day 13**, whose Dispatch content went to Day 8 beat 8 (see the Day 8 note): the number is free, the old content is not coming back. The only remaining **empty placeholder** is `ai-foundations/3-context-windows/` — an empty folder means "not started", not "lost work".

### `videos/ai-training/` is the authoritative series

**Each Day covers its topic in full, even where an older `claude/` (Module 2) video already covered it.** The `claude/` videos are the earlier per-feature walkthroughs; they do **not** limit or override what a Day says, and a Day must never be trimmed to a "see the other video" signpost. Duplication between a Day and Module 2 is expected and acceptable — a Day is meant to stand on its own. This **supersedes** the older "intro/overview videos signpost topics that have their own video" convention, which applies only within the `claude/` series.

That's why Day 3 carries Projects (beat 10) and memory (beats 11-12) at full depth despite `claude/2-projects` and the intro's Settings beat existing. When you extend a Day, ask "does a viewer who watches only this Day have everything they need?" — not "was this covered elsewhere?"

**Known conflict to fix in `claude/13-chatgpt-to-claude`:** its script says Claude Memory is "on by default (rolled out to all plans)". Per the support article, memory is **off by default on Team and Enterprise** plans, and Phlo is on a Team plan. **This is still unfixed** — the folder sat untracked on one machine until 2026-09-10 and was committed as-authored so nothing was lost; the narration line is unchanged and must be corrected before the video is recorded or re-recorded. Note also that Day 3 beats 11-12 and Day 8 both now state the Team/Enterprise default correctly, so this video is the only place the wrong claim survives.

**Day 3** is `videos/ai-training/3-prompting/build_excalidraw.py` → `prompting-crispe-framework.excalidraw`: one **sixteen-beat** board in the standard scaffold — orange *vague in, vague out* → green *the same ask, rebuilt* → violet *CRISPE* six dials → blue *fault-finder* → indigo *tell it why* → green *show, don't just tell* → red *say what to do* → blue *paste first, ask last* → teal *set once, never repeat* → orange *Projects hold the context* → indigo *it remembers you now* → red *you stay in charge of it* → violet *so does CRISPE still matter?* → teal *how the sharp end works* → indigo *different models, different habits* → yellow *capture by voice*, with demo badges on beats 2, 9, 10 and 12. **The framework is CRISPE** (Context, Role, Instructions, Style, Parameters, Example) — six letters, not the five-letter CRISP that older drafts of the curriculum mapping used. It briefly existed as two single-beat scenes (`build_prompt_anatomy.py`, `build_your_setup.py`); those were folded into the one board and deleted — one board per video is the convention, don't split a video across scene files again.

**Day 3's beats 5-8 and 10 are distilled from Anthropic's [Prompting best practices](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-prompting-best-practices) page and its per-model sub-pages, and the filter is deliberate: only what changes what a person TYPES goes on the board.** This audience uses the Claude app, not the API, so the API-only material is left off on purpose — `effort`, adaptive thinking, `budget_tokens`, `max_tokens`, prefill migration, computer-use toolsets, code-review harnesses, subagent caps and LaTeX. Don't "helpfully" add them back. Beat 15 (per-model habits: Claude Opus 5, Claude Sonnet 5, Claude Fable 5 / Mythos 5, Claude Opus 4.8) dates faster than anything else on the board and says so on the board; re-check it against the docs before re-recording. Model names are proper nouns — exact capitalisation, as with Claude and Artifact.

**Day 3's beats 11-12 cover Claude's memory and chat search** (source: `support.claude.com/en/articles/11817273`), running power-then-control: beat 11 is memory, chat search and per-Project memory spaces, tied back to CRISPE's Context dial; beat 12 is the controls (**Settings → Memory → Topics**, *Pause memory* vs *Reset memory*, the incognito ghost icon) plus the safety rule. Two facts here are easy to get wrong and are load-bearing for a regulated pharmacy: **memory is off by default on Team and Enterprise plans** (an owner enables it org-wide — so the board says why you might not see it, rather than promising it's on), and **health and other sensitive topics are excluded from memory by default** — the board tells staff to leave that setting alone, and that deleting a chat does *not* delete the memories it generated. Memory is also touched in `claude/1-claude-intro` (the Settings beat) and `claude/13-chatgpt-to-claude` (seeding it from ChatGPT), and that's fine — see the precedence rule below.

**Day 4** is `videos/ai-training/4-projects/build_projects.py` → `claude-projects.excalidraw`: one **seventeen-beat** board, the longest in the repo (~13 min) — red *the tax you pay every morning* → orange *it remembers you, not the job* → green *every chat starts briefed* → violet *what a Project actually is* → teal *three places context can live* → blue *the shift* → violet *the instructions field* → indigo *write it like you mean it* → green *three Projects you could build this week* → orange *the maths* → blue *what you get on your plan* → teal *why it's a multiplier* → violet *sharing, and who can do what* → indigo *let's build one - live* → yellow *pause here, and try it* → blue *slow vs live* → violet *one Project. Twenty minutes.*, with demo badges on beats 3 and 14. It **moved here from `videos/claude/2-projects/`** (was module 2.2, `phlo-2.2-claude-projects.excalidraw`) when the day-by-day series became authoritative — Day 3 teaches one good prompt, Day 4 teaches how to stop retyping it. If it needs a shorter cut, **beats 11 and 13 are the droppable pair** (plan limits, sharing mechanics); nothing later depends on them.

**Day 4's beats 5, 8, 11 and 13 are sourced from four documents and `projects_prompt.md` records exactly what was taken from each**: the [Claude Projects help-centre article](https://support.claude.com/en/articles/9517075-what-are-projects) (every plan including free, the five-Project free cap, retrieval at up to 10x as content approaches the context limit, "Can view"/"Can edit" and the org-wide sharing admin switch) and [Claude Code's memory docs](https://code.claude.com/docs/en/memory). **That is a different feature from the Claude memory on Day 3's beats 10-11**: Day 3 is the Claude app remembering you, Day 4 borrows only the instruction-writing discipline from `CLAUDE.md` files in Claude Code. Don't let the two merge. The memory doc is filtered the same way Day 3's is — **only what changes what a person TYPES**. Its instruction-writing discipline is on beat 8; the developer machinery (`.claude/rules/`, imports, `CLAUDE.local.md`, `claudeMdExcludes`, managed policy files, `/memory`, auto-memory settings) is deliberately **off** the board, because this audience uses the Claude app. Don't add it back. The plan and capacity figures date fastest — re-check them before re-recording. **Beat 5 and the rewritten beat 2 come from [Claude's memory](https://support.claude.com/en/articles/11817273) and [Claude's personalization features](https://support.claude.com/en/articles/10185728-understanding-claude-s-personalization-features)**: memory is **on by default on Pro and Max, off on Team and Enterprise until an owner enables it, and absent on Free**, each Project holds **its own separate memory space**, and **"Instructions for Claude"** (Settings) is account-wide on every plan. Beat 2 used to be headed *"every chat starts from zero"* with the caption *"no memory of yesterday's brief"* — **that was false and contradicted Day 3**; the board now says what is actually true, that what carries over is about *you*, not about *this job*. Don't let that claim back in — the same wording still sits in `ai-foundations/1-what-is-ai` (see Notes / known drift).

**Day 7** is `videos/ai-training/7-skills/build_skills.py` → `claude-skills.excalidraw`: one **six-beat** board, the shortest in the repo (~5 min) — orange *you have explained this four times* → violet *procedure, not context* → blue *search the gallery first* → teal *the hidden assumption* → red *make it critique its own steps* → green *test on a known input*, with one demo badge, on beat 5. It **moved here from `videos/claude/4-skills/`** (`build_skills_training.py` → `skills.excalidraw`) and was **cut from eleven beats to six** — Day 7 teaches the habit, not the feature surface. It is placed **before Cowork deliberately**: most jobs people bring to Cowork should have been a Skill. Everything displaced (the `SKILL.md` anatomy, the four types, progressive disclosure, the enable flow and its code-execution prerequisite, the feature-comparison table, the recap) is on `claude-skills-resource-card.md` — **that card is a deliverable, not an afterthought; don't re-inflate the board with it.**

Three Day 7 decisions that are load-bearing. **Beat 5 absorbs the retired reverse-prompting day**, and teaches *critique* only: "you are asking it to critique what you wrote - not to write it for you." **Never add "have Claude interview you" to this board or script as doctrine** — and note the board deliberately never names it, because naming a practice to warn against it teaches it. **The red data caution stayed on the board, on beat 3** ("A Skill is a shared file. Never put confidential or sensitive data inside one") — the six-beat brief had no slot for it, but it is the only on-screen data-handling rule in a mandatory course for a regulated pharmacy, so it moved rather than went. **One worked example (`SKILL_STEPS`, a monthly report) runs through beats 2, 4, 5 and 6**, with one hidden assumption — "we always exclude the trial accounts" — as the hole in beat 4, Claude's first finding in beat 5, and the £5,020 → £4,180 fix in beat 6; change the steps and all three must move together. Two colours are held constant and are never beat accents: **indigo = a Skill, violet = a Project folder** (the latter shared with Day 4). Unlike Day 3 and Day 4, **Day 7 has no curriculum entry and no source list** — its board and narration were written from the brief in `claude-skills-prompt.md`, which says so; the gallery name, the enable path and the plan availability date fastest and are flagged for re-check on the Resource Card.

**Day 8** is `videos/ai-training/8-cowork/build_cowork.py` → `claude-cowork.excalidraw`: one **nine-beat** board (~7 min 30) — orange *a conversation versus a shift* → violet *the brief is the whole job* → blue *what a brief must name* → teal *checkpoints, applied* → indigo *it splits the work* → red *long runs hide their mistakes* → yellow *it has its own browser* → green *start it from your phone* → orange *what belongs here*, with one demo badge, on beat 4. It **moved here from `videos/claude/11-cowork/`** (was module 2.11) when the day-by-day series became authoritative, was **rewritten from a feature walkthrough into an argument about supervision**, and **absorbs Dispatch from the retired day 13** (beat 8) — that is **day 13 of the day-by-day series**, not `claude/13-chatgpt-to-claude`, which is a different video in a different numbering namespace and is unaffected by this move (its memory conflict, noted above, still stands). If it needs a shorter cut, **beats 7 and 8 are the droppable pair** (built-in browser, Dispatch); nothing later depends on them. Beat 6 — the compounding-error beat — is the one to protect.

**Day 8's beat 4 guesses, deliberately and in one place.** It is a callback to the three checkpoints on the **day 6 autonomy-ladder board, which does not exist yet**; the three stops (*before the first write*, *at the join*, *before it leaves the folder*) were derived from the Day 8 brief and are pinned in a single `STOPS` constant at the top of the build script, so reconciling with day 6 is a one-line edit. **Check them against day 6 before recording.** Manual/Auto/Skip sits underneath as the setting that implements them — it is the mechanism, not the three stops.

**Day 8's beats 4, 5, 7 and 8 are sourced from five Cowork documents and `claude-cowork-prompt.md` records exactly what was taken from each** (Get started with Claude Cowork; the Cowork overview docs; Use the built-in browser; Set up browser use for Team and Enterprise; Let Claude use your computer; Assign tasks from anywhere). Filtered the same way Day 3 and Day 4 are — **only what changes what a person types or decides**; Plugins, monitoring, the Chrome side panel, cookie importing and the Claude Code half of Dispatch are deliberately off the board. **Two facts here are load-bearing and were corrected against the brief, which had both wrong:** computer use is **beta, Pro and Max only, and not available on Team or Enterprise** (not a "research preview"), and Dispatch **runs locally on your desktop** — awake, with Claude Desktop open — and is a **limited beta on Pro and Max** (not "in the cloud"). Since Phlo is on a Team plan, the board says why you may not see either, rather than promising them — the same treatment Day 3 gives memory. **The plan figures on this board date faster than on any other in the repo** (three features, three plan rules, two betas): re-check before re-recording.

**Day 8's demo badge is on beat 4, not beat 8, and that is deliberate.** The brief put it on beat 8 ("dispatch a real multi-file task, then stop it at the first checkpoint"), but Dispatch is Pro/Max only, so that cut-away is likely un-recordable on Team. The badge keeps the payload — stopping a real multi-file run at its first checkpoint, in Manual mode — and moves to the checkpoints beat. If Dispatch reaches Team, **add** a badge to beat 8 rather than moving this one back.

**Day 10** is `videos/ai-training/10-design/build_design.py` → `claude-design.excalidraw`: one **seven-beat** board (~6 min 38) — orange *blank page to worth-reacting-to* → violet *reference beats adjectives* → blue *structure versus polish* → teal *iterate by talking to it* → indigo *in units, not wholesale* → red *presentable is not correct* → green *human pass for brand-final*, with one demo badge, on beat 7. It **moved here from `videos/claude/12-design/`** (was module 2.12, the Claude Design product walkthrough) and was **adapted from nine product beats to seven practice beats** on one argument: *the advanced failure in design work is not a bad output, it is an acceptable one — generic passes review.* The board names **no product, no plan tier and no model**, deliberately, so it does not date; its demo badge cuts to the **Claude Desktop** app. It sits **after Cowork on purpose**: the habit it teaches — name the test before you look — is what catches a *plausible* result, which is exactly what a long unsupervised run produces. **Day 9 does not exist and nothing on this board depends on it.** If it needs a shorter cut, **beats 3 and 4 are the droppable pair** (~5 min 40, inside the 4-to-6 target); **beat 6 is the one to protect**. **Day 10's board was regenerated on 2026-09-14** when design.md moved to Day 12: the beat 2 chip now reads "Day 12, design.md". The replacement string is the same length and the seed is unchanged, so the scene diff is those two strings and nothing moved — **if this video is already recorded, a line in the Loom description is enough; it does not need a re-record.**

Three Day 10 decisions are load-bearing. **The red data caution sits on beat 7 rather than on the Resource Card** — the seven-beat brief had no slot for it, but it is the only on-screen data-handling rule in a mandatory course for a regulated pharmacy, the same call Day 7 made with its Skill caution; don't quietly drop it. **Beat 6 (the adversarial beat) carries its own catch** — three interchangeable "acceptable" outputs, then *name what it has to get right before you look at it* as one falsifiable sentence, then all three fail it; a failure with no catch is a diagnosis with no test, and this beat is the Best Catch feeder. **The human gate uses a bespoke `human_gate()`, not the kit's `person()`**, which is sized for a crowd of tiny figures and reads as a paper dart standing alone — that marker is safety-bearing, so it has to be unmistakable (the same reasoning as Day 3's `person_big()`). **One worked example** (a generic landing page; `NAMED_CHANGES` and `CRITERION` pinned at the top of the build) runs through beats 1, 4, 5 and 7 — change it and all four must move together.

**Day 10 has no curriculum entry**, and the module-2.12 entry that exists describes the product walkthrough this replaces, so board and narration were written from `claude-design-prompt.md` — reconcile before recording. Everything displaced by the reframe (research-preview status, the model and plan, design-system onboarding, the four inputs, PPTX / Canva export, the three role flows) is on `claude-design-resource-card.md`; **that card is a deliverable, not an afterthought — don't re-inflate the board with it.** Day 10 also records a process lesson worth keeping repo-wide: its first narration draft measured **1,520 words — over 10 minutes, past the 8-minute cap** — because the per-beat *second* budget was never checked back against the written prose. `claude-design-prompt.md` carries the per-section word-count table and the command to re-measure; **do that on every script before calling it done.**

**Day 11** is `videos/ai-training/11-design-workflows/build_design_workflows.py` → `claude-design-workflows.excalidraw`: one **nine-beat** board (~6 min 42) — orange *you already have the habits* → violet *the loop, six steps* → blue *a spec in, wireframes out* → teal *hand it to whoever builds it* → indigo *explore before you commit* → orange *one input, many outputs* → yellow *motion graphics for video* → red *it checks its own work* → green *where it stops, and who owns it*, with one demo badge, on beat 8. **It is topic coverage, not an argument** — Day 10 is the argument about judgement and Day 12 is the plumbing; this Day walks the actual jobs, built on one repeatable six-step loop. It took slot 11 as new work, which is why `11-design-md/` became `12-design-md/`. If it needs a shorter cut, **beats 4 and 6 are the droppable pair** (the handoff, many outputs; ~5 min 42); **beat 8 is the one to protect**.

**Day 11's beat 1 is a deliberate 20-second re-run and must never grow into one.** The brief was "ignore anything already covered in the Day 10 / design.md boards, cover those as a brief reminder, then everything new from wireframing onwards" — so beat 1 is four Day 10 chips plus two forward chips (Day 12 for the three files, Day 12 + Claude PowerPoint for a full deck), and this board's own subject starts at beat 2. That is **not** a breach of the precedence rule: the rule governs a Day's *own* subject, and Day 10's beat 2 → Day 12 chip is the standing precedent for a forward pointer.

**ONE NUMBERED PROCESS PER BOARD.** The six-step loop is beat 2 and it is the only numbered thing on this board. **Beat 5's exploration habit is a numbered sequence in the source and is drawn UNNUMBERED on purpose** — two competing "the steps are" claims on one board is a legibility failure, and what travels is the habit (references in, three directions, then combine the parts that earned their place), not the count. Don't number it in a later edit.

**Day 11 has three sources and they are not equal: two third-party (a video transcript and a published workflow playbook) and Anthropic's own documentation, which outranks both.** `claude-design-workflows-prompt.md` records the mapping line by line, and both third-party sources were distilled in our own words. **Claims that survive only in a third-party source stay off the board**: a printable 3D-object download and a home-screen template gallery are on `claude-design-workflows-resource-card.md`, marked unverified. **One earlier claim is now resolved as false** — the transcript showed generated animations downloading as an MP4; the playbook states there is no native export and screen recording is the workaround, so beat 7 carries the durable half (*it plays in the browser, so you capture it off the screen*). **That is the fastest-dating line on the board and the docstring says so** — same treatment Day 3's beat 15 gets. **One claim was changed on safety grounds**: the transcript's answer to the image limit is "go to another vendor's model", and that routing decision belongs to the Phlo AI Use Policy, so beat 6 says "a real one, or one from a tool you are allowed to use" and names no third-party model.

**Day 11 states its plan fact positively, and that is the opposite of Day 3 and Day 8.** Those two hedge ("you may not see this") because memory and Cowork are **off** by default on Team. **Claude Design is default ON for Team** and default off for Enterprise, and **there is no separate Claude Design usage allowance** — it draws from the same pool as chat, Claude Code and Cowork. Both facts are on the card, not the board.

**Day 11's adversarial beat is 8, and its catch is docs-backed.** Step 5 of the loop tells you to ask Claude to review its own work, and it does that genuinely well — readability, hierarchy, contrast, accessibility — while a second check you never see holds output against your design system. Both are worth having; **neither is a second opinion**, because it reviewed the screen it drew using the data it invented. The catch is to make it render the states it never drew (*empty, refused, forty rows with a sixty-character name*), which is Anthropic's own design-review guidance written as one falsifiable sentence in the shape Day 10's beat 6 established. **The demo badge sits on beat 8** because that is the payload and it is fast on camera, unlike a generation. **One worked example** (an internal holiday-request tool, pinned in `TOOL` and `CATCH`) runs through beats 2, 3, 4 and 8, and beat 8's three states are exactly the states beat 3's wireframe never drew — change the tool and all four move together. **The red data rule sits on beat 9**, the same call Day 7 and Day 10 each made. **Green = the human pass is held constant across Day 10, Day 11 and Day 12**; Day 11 deliberately starts no second object-colour system, because Day 12 already owns blue = design.md, green = design system, teal = template.

**Day 12** is `videos/ai-training/12-design-md/build_design_md.py` → `claude-design-md.excalidraw`: one **eight-beat** board (~7 min 10) — orange *start in the wrong place* → violet *three files, before you prompt* → blue *design.md is the rulebook* → green *the design system makes it native* → teal *the template is the layout* → indigo *two kinds of feedback* → red *everything compounds, mistakes too* → yellow *then it leaves the room*, with one demo badge, on beat 6. **It is the depth pass on the `design.md` that Day 10 only points at**, and the split was deliberate: Day 10 teaches the judgement, Day 12 teaches the plumbing that makes the judgement permanent. **Day 10's beat 2 carries a one-line pointer chip to Day 12 and Day 12 never points back** — a forward pointer to another Day's topic, which is not the same as trimming a Day's own subject, so the precedence rule holds. That pointer added 15s to Day 10's runtime (now ~6 min 53).

**Day 12 was distilled from a third-party tutorial transcript and `claude-design-md-prompt.md` records line by line what was taken and what was dropped.** Nothing came from Anthropic documentation, so **every product-specific detail is on `claude-design-md-resource-card.md`, not on the board** — the model picker and effort setting, the 10-20 minute build, the "create design system" flow, the project-tree navigation, the clarifying questions, the export routes and presenter view. The board deliberately names **no plan, no model and no button**, which is why it does not date; **fix the card, not the board.** Note the transcript's "effort" is Claude Design's own UI control, **not** the API `effort` parameter Day 3 deliberately excludes — don't let that exclusion bleed across.

**One departure from the Day 12 source is not negotiable.** The tutorial downloads a real brand's `design.md` from a public repository, has another model "remove the proprietary content and replace them using your own judgment", renames it, and feeds that in — explicitly *because* Claude will not reproduce a real brand's guidelines. **That is routing around a refusal and it is off the board, out of the script and out of the demo steps.** What survives is the mechanism (start from a *reference* design.md, not a blank page) plus the red rule on beat 3, stated positively: the refusal is correct behaviour, take your rules from your own brand assets, and **if you cannot say where a colour came from, it is not yours.** Do not put the laundering step back.

Three more Day 12 decisions are load-bearing. **The adversarial beat is 7 and it is deliberately NOT the brand question** — laundering fails *loudly*, because Claude refuses; the silent failure is a wrong rule saved into the design system, or bad feedback captured into the project's `CLAUDE.md`, propagating to every future design and every new chat with nothing on screen to say so. Its catch is a regression check — **keep one reference design and re-render it after every change** — which is Day 7's "test on a known input" pointed at a design system. **Beat 6 carries a three-way name collision that must stay on the board**: Claude's memory (Day 3, about *you*), `CLAUDE.md` in Claude Code (Day 4, a codebase) and `CLAUDE.md` in a design project (here, these designs). The repo already warns the first two must not merge; this is the third, and beat 6 is the only place a viewer sees all three side by side — **don't remove that row to save space.** **Four colours are held constant**: blue = the design.md, green = the design system, teal = the template, and green **also** = the human pass on beat 8, held from Day 10's closing beat so the rule reads as the same rule across both boards.

**"Separate frames for the video" means separate beat slots, never Excalidraw frames.** A literal `frame` element is a hard Style-B guard failure (it is the rejected Style A's signature). The six beats sit in 1200-wide slots with `GAP = 800` of whitespace between them, and you frame one at a time by panning — the whitespace is the camera. Likewise there is no animation to author: where a brief asks for something "revealed one by one", bake the order into the board (numbered badges, reading order) and document the pan order in the build script's docstring.

## Non-negotiable conventions (from `.claude/skills/SKILL.md`)

Read `.claude/skills/SKILL.md` in full before authoring a diagram. The hard rules:

- **Brand colours are placeholders.** The HEX block in `SKILL.md` and the palette constants at the top of each `build.mjs`/`build_excalidraw.py` are placeholder values, **not** real Phlo brand colours, until someone swaps in the design-system hex codes. Don't assume the on-screen palette is final.
- **Safety (regulated pharmacy):** no real patient identifiers (use `Patient_001`, `J. Doe`, `NHS_TEST_123`); always draw the human verification gate on any clinical/dispensing/records workflow; AI never writes to patient records directly (AI drafts → human approves → record updates); label MHRA/GPhC/CQC steps; no UI screenshots — diagrams are abstractions.
- **Vocabulary is fixed:** "Phlo" not "we/us"; "Patient" not "user/customer"; "Claude"/"Granola"/"Beacon"/"MCP"/"Claude Code" are proper nouns with exact capitalisation.
- **Pacing is now manual.** With static scenes recorded by panning, "animation" means the camera move you perform live in Loom — reveal each beat by panning to it, hold ~3s of narration per beat, left-to-right. The SKILL.md "Animation defaults" (stagger, `drawProgress`, keyframes, MP4 export) describe the **legacy Excalimate** flow only and don't apply to static builds.

## Notes / known drift

- The skill lives at `.claude/skills/SKILL.md` (skill name is still `phlo-learn-videos`) and work is organised under `videos/<series>/<NN-slug>/`. README and SKILL.md have both been brought in line with this and with the static/flowing workflow.
- `Phlo_Mandatory_AI_Course_Curriculum.docx` is the course's source of truth for every video's script blocks, but it is **not** checked into this repo — ask for it before authoring content.
- **Ownership, to stop drift recurring:** `.claude/skills/SKILL.md` owns the drawing rules, safety guardrails, and vocabulary; `README.md` owns the human production workflow; this file is the operating map and cross-references the other two rather than restating them. SKILL.md uses the lively non-brand Excalidraw palette deliberately — there is no "replace the brand hex" step.
- `scripts/compile_excalimate.py` (legacy) hardcodes `llm_explainer.*` paths relative to repo root.
- **`claude/1-claude-intro`'s roadmap signpost is stale in four places, and is still NOT fixed.** The video signposts "the rest of this module" as Projects, Artifacts, Skills, Scheduled tasks, Connectors (MCP) and Cowork — but **Projects is Day 4, Skills is Day 7 and Cowork is Day 8**, so three of those six are no longer in Module 2 at all. It appears in the chip row at `build_claude_interface.py:495`, in the closing line at `build_claude_interface.py:505` ("Next: Projects"), and twice in `claude_intro_script.md` (lines 43 and 67, plus the "next video" references on lines 11, 57, 71 and 85). The 2026-09-10 rewrite of this video regenerated the board and narration but **did not** touch the signpost, so this survived it — the line numbers above are post-rewrite. Fixing it means regenerating `claude_intro.excalidraw` and rewriting narration for a video that may already be recorded, so it is left alone deliberately — **decide whether to re-record before touching it**. When you do: drop the `Projects`, `Skills` and `Cowork` chips, and point those lines at the `ai-training/` Days instead of "later in this module".
- **`ai-foundations/1-what-is-ai` scene 6 was hand-edited on 2026-09-02** — it has no `build*.py`, so there was nothing to regenerate. Three text elements only (`s6_title`, the Conversation 2 label, `s6_callout_text`), each replaced with a same-length-or-shorter string so no element outgrows its declared box; palette and fonts untouched, guard still passes. It used to be titled **"No memory by default"** and claimed Claude starts every session blank, which is no longer true. The scene's *mechanism* was always right (the model is stateless; Projects and memory re-feed context) — only the framing was wrong, so the fix renamed the scene to "Memory is a layer, not the model", relabelled the second conversation "(memory off)", and folded Claude's memory into the callout. **If this video is ever given a `build*.py`, port these three strings across** or the old claim comes back.
- **All twelve Module 2 Claude videos were built** with Style B `build_*.py` scripts that live next to their video and write output via `os.path.dirname(__file__)` (lands in-folder): `1-claude-intro/build_claude_interface.py` → `claude_intro.excalidraw`; `3-artefacts/build_excalidraw.py`, then `build_scheduled_tasks.py`, `build_connectors_mcp.py`, `build_powerpoint.py`, `build_excel.py`, `build_word.py`, `build_web_extension.py`, and `13-chatgpt-to-claude/build_chatgpt_to_claude.py` → `chatgpt-to-claude.excalidraw` (a **thirteenth** video, added after the original twelve). **`2-projects/`, `4-skills/`, `11-cowork/` and `12-design/` are no longer here** — they became **Day 4**, **Day 7**, **Day 8** and **Day 10**, and live at `videos/ai-training/4-projects/`, `videos/ai-training/7-skills/`, `videos/ai-training/8-cowork/` and `videos/ai-training/10-design/` (see the Day 4, Day 7, Day 8, Day 10 and Day 12 notes above). `2-projects/` was **converted from a rejected frame-based "Style A" deck** (10 fixed `1920x1080` frames, purple/lilac palette); that old generator (`build_projects_2_2.py`) has been deleted — do not reintroduce a frame-based build.
- `videos/claude/1-claude-intro/` has a single canonical scene, `claude_intro.excalidraw`, generated by `build_claude_interface.py`. (The script's docstring + `claude_intro_prompt.md` still say `claude-interface-properly.excalidraw`; the live `out =` target is `claude_intro.excalidraw` — trust the code. Regenerate from the script; don't keep a second hand-named copy.)
- Several `*_prompt`/`*_prompt.md` files (e.g. `1-claude-intro/claude_intro_prompt.md`) still describe the **old editorial Style A** (fontFamily 2, five `1600x1000` frames, purple brand palette). The shipped build scripts are Style B; trust the `build_*.py` + script over the prompt file when they disagree.
```
